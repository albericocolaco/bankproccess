{
  "name": "oauth2",
  "version": "1.0.0",
  "description": "Authentication",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "start": "nodemon --watch src/**/*.ts --ignore src/**/*.spec.ts --inspect=8001 -e ts,tsx --exec node -r ts-node/register ./src/index.ts",
    "build-dev": "rm -rf dist && webpack --env.APP_TARGET dev --mode development",
    "build-hml": "rm -rf dist && webpack --env.APP_TARGET hml --mode production",
    "build-prd": "rm -rf dist && webpack --env.APP_TARGET prd --mode production",
    "start-dev": "npm run build-dev && node dist/server",
    "start-hml": "npm run build-hml && node dist/server",
    "start-prd": "npm run build-prd && node dist/server"
  },
  "author": "alberico.rodrigo@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.6",
    "@types/helmet": "0.0.45",
    "@types/jsonwebtoken": "^8.3.9",
    "body-parser": "^1.19.0",
    "bunyan": "^1.8.12",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "healthcheck-middleware": "^1.0.1",
    "helmet": "^3.22.0",
    "jsonwebtoken": "^8.5.1",
    "swagger-ui-express": "^4.1.4"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.11.0",
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "supertest": "^4.0.2",
    "ts-loader": "^6.2.2",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11",
    "webpack-node-externals": "^1.7.2"
  }
}
